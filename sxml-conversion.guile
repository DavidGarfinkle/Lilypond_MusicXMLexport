#! /usr/local/bin/guile -s 
!#
(use-modules (sxml match))
(load "helloworld-line.scm")


(define x '(d (a 1 2 3) (a 4 5) (a 6 7 8) (a 9 10)))

(sxml-match x
  [(d (a ,b ...) ...)
   (list (list b ...) ...)])
#! 
(sxml-match hw
	((music (@ (name)))
		(list name)))
	[(music (@ (name TimeSignatureMusic)) (numerator n) (denominator d) ...)
		`((time (beats ,n) (beat-type ,d)))])
!#
